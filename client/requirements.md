## Packages
react-webcam | For accessing the camera feed
@tensorflow/tfjs | Core machine learning library
@tensorflow-models/coco-ssd | Object detection model for person detection
recharts | For visualization of occupancy history and analytics
framer-motion | Smooth animations for UI elements
date-fns | For formatting timestamps

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
